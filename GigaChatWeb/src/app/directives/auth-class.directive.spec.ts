import { AuthClassDirective } from './auth-class.directive';

describe('AuthClassDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthClassDirective();
    expect(directive).toBeTruthy();
  });
});
